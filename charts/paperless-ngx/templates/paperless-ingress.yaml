{{- if .Values.paperless.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "paperless-ngx.fullname" . }}
  labels:
    {{- include "paperless-ngx.labels" . | nindent 4 }}
  {{- with .Values.paperless.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.paperless.ingress.className }}
  ingressClassName: {{ .Values.paperless.ingress.className }}
  {{- end }}
  {{- if .Values.paperless.ingress.tls }}
  tls:
    {{- range .Values.paperless.ingress.tls }}
    - {{- if .hosts }}
      hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.paperless.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "paperless-ngx.fullname" $ }}
                port:
                  number: {{ $.Values.paperless.service.port }}
          {{- end }}
    {{- end }}
{{- end }}

